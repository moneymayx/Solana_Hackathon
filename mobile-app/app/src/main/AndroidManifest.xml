<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Biometric authentication -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    
    <!-- Solana Mobile Wallet Adapter -->
    <queries>
        <intent>
            <action android:name="solana.mobilewalletadapter.action.AUTHORIZE" />
        </intent>
    </queries>

    <application
        android:name=".BillionsApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BillionsBounty"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        
        <!-- Mobile Wallet Adapter metadata -->
        <meta-data
            android:name="com.solanamobile.mobilewalletadapter.ENDPOINT"
            android:value="https://billionsbounty.com" />
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.BillionsBounty">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
